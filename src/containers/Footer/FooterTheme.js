const theme = theme => ({
	footerWrapper: {
		width: '100%',
		height: '50px',
		backgroundColor: theme.colors.blackoliveDark,
		position: 'fixed',
		bottom: '0',
		zIndex: 1,
	}
});

export default theme;